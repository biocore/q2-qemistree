from skbio import TreeNode

# something like this
# str(representative_sequences.file.view(DNAFASTAFormat)


def make_hierarchy(collated_fingerprints: str=None, threshold: float=0.5) -> TreeNode:
    pass
